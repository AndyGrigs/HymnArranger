# HymnArranger — правиловий генератор баянних аранжувань
# Python 3.10+
#
#   python3 -m venv .venv
#   source .venv/bin/activate          # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
#   python arranger.py melody.musicxml -p mixed -o out.musicxml
#   python arranger.py melody.musicxml --all  -o out.musicxml

# HymnArranger — генератор баянних аранжувань
# Python 3.10+
#
#   python3 -m venv .venv
#   source .venv/bin/activate          # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
#   python arranger.py --suite --seed 42     # CLI
#   python server.py                          # HTTP, http://127.0.0.1:8000/docs

# ── Ядро: обробка нотного матеріалу ───────────────────────────────
music21==10.5.0

# ── HTTP-сервер ───────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32      # завантаження файлів через form-data

# ── База даних ────────────────────────────────────────────────────
sqlalchemy==2.0.41
psycopg2-binary==2.9.10       # PostgreSQL-драйвер
alembic==1.16.1

# ── Авторизація ───────────────────────────────────────────────────
bcrypt==4.3.0
PyJWT==2.10.1
python-dotenv==1.1.1

# ── Email (Brevo) ─────────────────────────────────────────────────
sib-api-v3-sdk==7.6.0

# ── Pydantic extras ───────────────────────────────────────────────
email-validator==2.2.0        # потрібен для pydantic EmailStr

# ── Розробка і тести ──────────────────────────────────────────────
pytest==9.1.1
httpx2                        # потрібен для starlette.testclient (starlette 1.x)