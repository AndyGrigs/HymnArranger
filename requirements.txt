# HymnArranger — правиловий генератор баянних аранжувань
# Python 3.10+
#
#   python3 -m venv .venv
#   source .venv/bin/activate          # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
#   python arranger.py melody.musicxml -p mixed -o out.musicxml
#   python arranger.py melody.musicxml --all  -o out.musicxml

# HymnArranger — генератор баянних аранжувань
# Python 3.10+
#
#   python3 -m venv .venv
#   source .venv/bin/activate          # Windows: .venv\Scripts\activate
#   pip install -r requirements.txt
#
#   python arranger.py --suite --seed 42     # CLI
#   python server.py                          # HTTP, http://127.0.0.1:8000/docs

# ── Ядро: обробка нотного матеріалу ───────────────────────────────
music21==10.5.0

# ── HTTP-сервер ───────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.1
python-multipart==0.0.32      # завантаження файлів через form-data

# ── Розробка і тести ──────────────────────────────────────────────
pytest==9.1.1
httpx==0.28.1                 # потрібен для fastapi.testclient